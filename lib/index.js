const {
  createRequestCorrelatorMiddleware,
  createRequestCorrelatorMock,
} = require('./express-request-correlator');

module.exports = {
  createRequestCorrelatorMiddleware,
  createRequestCorrelatorMock,
};
